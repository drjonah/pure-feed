{
  "name": "pure-feed-extension",
  "version": "0.2.7",
  "private": true,
  "scripts": {
    "setup": "npm install && cd options && npm install && cd .. && node build.mjs && cd options && npm run build",
    "build": "node build.mjs && cd options && npm run build",
    "package": "node build.mjs && cd options && npm run build && cd .. && node package.mjs"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^4.22.0",
    "nsfwjs": "^4.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
