{
  "name": "pure-feed-extension",
  "version": "0.2.7",
  "private": true,
  "scripts": {
    "setup": "npm install && cd options && npm install && cd .. && npm run build",
    "build": "cd options && npm run build && cd .. && node build.mjs",
    "build:chrome": "cd options && npm run build && cd .. && node build.mjs --browser=chrome",
    "build:firefox": "cd options && npm run build && cd .. && node build.mjs --browser=firefox",
    "package": "npm run build && node package.mjs",
    "package:chrome": "npm run build:chrome && node package.mjs --browser=chrome",
    "package:firefox": "npm run build:firefox && node package.mjs --browser=firefox"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^4.22.0",
    "nsfwjs": "^4.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
