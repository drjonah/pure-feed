# v0.2.6

## Bug Fixes

- Fix Chart.js Filler plugin not registered, causing console errors on the Stats page when the line chart's `fill` option was enabled
- Fix Stats page layout: summary cards now use a balanced 3+2 grid instead of awkward 4+1
- Fix "By Label" and "By Platform" charts overflowing page width
